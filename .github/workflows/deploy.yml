name: Upload SSIIC Website

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Deploy static site to S3 bucket
        run: aws s3 sync ./src/ s3://ssiic-230490046706 --delete

    #- name: Run invalidation for Cloudfront
    #  run: aws cloudfront create-invalidation --region ap-south-1 --distribution-id E35KT7I6V9QSID --paths "/*"